import MeanderStrip from "@/components/ui/MeanderStrip";

/** Gold Greek-key band between full-screen hero and the first content section. */
export default function HeroContentMeander() {
  return <MeanderStrip layout="fullBleed" track="deep" />;
}
