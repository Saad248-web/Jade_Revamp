/** Resend total attachment budget ~40 MB; cap partner photos per send. */
export const MAX_PARTNER_PHOTO_ATTACHMENTS = 5;
export const MAX_PARTNER_PHOTO_BYTES_EACH = 8 * 1024 * 1024;
export const MAX_RESUME_ATTACHMENT_BYTES = 4 * 1024 * 1024;
