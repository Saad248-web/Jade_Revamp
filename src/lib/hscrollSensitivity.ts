/** Native horizontal rail drag / wheel gain (category tabs, amenity rows, etc.). */

/** Pointer drag on `.jade-hscroll-track` — 1px finger ≈ 1.35px scroll. */
export const HORIZONTAL_RAIL_DRAG_GAIN = 1.35;

/** Two-finger / wheel horizontal delta multiplier on marketing rails. */
export const HORIZONTAL_RAIL_WHEEL_GAIN = 1.35;
